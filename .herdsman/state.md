# herdsman state

Live build state. **Written only by the Orchestrator**, at dispatch and at merge.
Task status is here, never in `docs/plan/`. See `docs/plan/README.md` for the
plan layout and `.claude/skills/herdsman/` for the workflow.

## Now

**Phases 0–8 DONE. Roadmap PAUSED after Phase 8 (user decision 2026-09-07 — see `docs/plan/decisions.md`).** Near-term work is testing / functionality hardening / refinement of the current scope — **the herdsman phase-slice cadence is on hold**, no new phase expanded or dispatched. Phases 9–13 stay as the eventual roadmap. Hardening tracked against `docs/plan/backlog.md` + the per-slice §9 follow-up list, not as dispatched slices.

**Release `v1.2.0` — RELEASED 2026-09-07.** `chore: release v1.2.0` (#89) squash-merged as `d8fb4f7` on `main` (folded the Phase 8 DONE-lag backfill + this pivot's doc edits + `app/pubspec.yaml` `1.1.0+3` → `1.2.0+4`). Tag `v1.2.0` cut from `d8fb4f7`; `release.yml` built + published the **signed** `app-release.apk` (73,138,505 B) to <https://github.com/Abbo0dio/olf/releases/tag/v1.2.0> (not draft/prerelease, auto-generated notes). Brings the downloadable build up from `v1.1.0` (which predated Phases 6–8) to everything on `main` through the Phase 8 close. Minor bump per the 2026-09-04 versioning policy (new features shipped: Phases 6, 7, 8; still `1.x` alpha). `cut_release.sh` bailed twice on `api.github.com` network timeouts at its `--watch` step — CI on #89 was green (all 9), so the merge → tag → release-monitor were finished by hand. Local branch `chore/release-v1.2.0` deleted; **remote `chore/release-v1.2.0` stays for user prune.** On-device smoke items in `docs/release-checklist.md` (p5.4 discreet icon, p6.2/p6.3 health bridges, p6.5 doctor report) need real hardware — user's to run.

Phase 8 — Passive wearable integration — **CLOSED 2026-09-07** (#88 `719ad04`, exit gate MET). **`schemaVersion` 11** (v10 = `bbt_entries.measurement_kind` p8.1a; v11 = `source_device` on `daily_flows` + `bbt_entries` p8.2; p8.5 + p8.6 added no schema). **Phase 8 shipped:** p8.1a #84 `5bee215` (Apple Watch wrist-temp path) · p8.2 #85 `c36836f` (Oura/Garmin via platform + `source_device`) · p8.5 #86 `3f41629` (passive cycle-phase inference, backtested, correctable) · p8.6 #87 `cb0a711` (multi-source arbitration — precedence policy in the reconciler, no schema). **p8.1b (watchOS companion) + p8.3 (direct cloud API) DEFERRED to backlog.** Per-slice record frozen in `phase-08.md` (### Notes table). Phase 7 CLOSED (#83 `8f9a341`). Workers 1/2/3 all idle with stale worktrees; phase-boundary memory write done 2026-09-07.

## Workers

| W | agent    | tab    | status   | slice | worktree        |
| 1 | worker-1 | w6:t2  | idle     | —     | ../olf-wt/p8.6 (stale — merged #87) |
| 2 | worker-2 | w6:tA  | idle     | —     | ../olf-wt/p7.6 (stale — merged #82) |
| 3 | worker-3 | w6:tB  | idle     | —     | ../olf-wt/p7.8 (stale — merged #78) |

All three Workers idle with stale worktrees — torn down at their next dispatch.

## Tasks

Current phase (8). Phase 7 per-slice record is **frozen in `phase-07.md` (### Notes
table)** — all 8 mode slices + close DONE (#73–#83, close `8f9a341`), dropped from
here at the Phase 7 close. Phase 6 frozen in `phase-06.md`, Phase 5 in `phase-05.md`.
Full Phase 8 slice scope: `docs/plan/phases/phase-08.md`.

| ID | status | PR | SHA | log |
| p8.1a | DONE | #84 | `5bee215` | Merged 2026-09-06, all 9 CI checks green (incl. `Build (macos-latest, ios)` — Swift bridge compiles). Apple Watch **passive wrist-temperature data path**, no companion app, no new dep. iOS bridge maps `HKQuantityTypeIdentifier.appleSleepingWristTemperature` (read-only, `#available(iOS 16)` guarded, no write/share auth) → `HealthSampleType.wristTemperature`; `health_import.dart` re-types to `basalBodyTemperature` **only at the reconcile boundary** (unchanged `ImportReconciler` `(type,day)` match), stores the row `measurementKind: sleepingWrist`, never pushes it back out. **§5 negotiation RESOLVED 2026-09-06 (Orchestrator "go") — one additive column, `schemaVersion` 9→10:** `measurement_kind TEXT NOT NULL DEFAULT 'basal'` on `bbt_entries` (`enum BbtMeasurementKind {basal, sleepingWrist}`); **rejected** a dedicated `wrist_temperature` table (would force a reconciler change) and unmarked `tempCelsius` reuse. **Condition MET — zero behaviour change:** all 5 `bbt_entries` temp readers filter `measurement_kind = 'basal'` (`thermalShift`, `dailyFertilityScore`, `bbt_chart`, `clinical_report` temp series, `drift_logging_activity_repository`); a `core` test asserts mixed-kind history === basal-only for shift / score / report. `source_device` **deferred to p8.2**; HRV/sleep **deferred to p8.5**. Migration package shipped in-PR: g.dart regen + real `schema dump` → `drift_schema_v10.json` + `generated/schema_v10.dart` + `_dumpedVersions += 10`, `migration_matrix_test` → v10 (`measurement_kind=='basal'` on pre-existing rows + `sleepingWrist` backup round-trip), new `wrist_temp_migration_test.dart` (v9 on disk → v10, `PRAGMA table_info`). `bbt_entries` already in `BackupService.tableOrder` + `RetentionService.deleteWhere` — no additions. `screen_nav` surface inventory 40→41. threat-model review-log entry + `local-database.md` "Schema v10". **1 `// SHORTCUT`:** conflict-review "take incoming" for a wrist-vs-manual conflict stores it as `basal` (pure `ReconciliationConflict` carries no kind) — rare manual path, re-correctable, deferred to p8.5. Depends p6.1, p6.2, p1.6. |
| p8.1b | DEFERRED | — | — | **Deferred to backlog** at the Phase 8 close (user's call 2026-09-07). Conditional slice — the exit gate ships it only "if its §5 plumbing clears"; the four hard requirements are met without it. watchOS SwiftUI companion + complication, App Group / `WatchConnectivity`, no network. New Xcode target + HealthKit entitlement + CI compile lane. `backlog.md` + `phase-08.md` #### p8.1b. |
| p8.2 | DONE | #85 | `c36836f` | Merged 2026-09-06, all 9 CI checks green (incl. both builds). **Oura + Garmin via the health platform** + device provenance — no vendor SDK, no OAuth, no network, no new permission (`pubspec.lock` / manifest / entitlements / plist all untouched). Nullable `source_device` TEXT on `daily_flows` + `bbt_entries`, `schemaVersion` 10→11, migration `if (from < 11 && to >= 11) { if (from >= 3) …dailyFlows; if (from >= 5) …bbtEntries; }` (`daily_flows` v3 / `bbt_entries` v5). iOS `withDevice()` reads `HKSource.name`/`HKDevice.name` off already-fetched samples; Android reads `dataOrigin.packageName`; `RawHealthSample.toWire()` never emits it (inbound provenance only). `device_label.dart` pure prettifier; `contributingDevicesProvider` + per-device "Apps & export" list (`reduceSpokenDetail`-redacted, hidden when all-manual). **§5 negotiation RESOLVED 2026-09-07 (Orchestrator "go", option A):** one minimal additive `ImportReconciler` clause — `sourceDevice` threaded through `HealthSample`/`LocalSampleView`; `inserts` indexed into `byTypeDay` as they accumulate; `ConflictReason.crossDeviceDisagreement` for a `(type,day)` match that is same `source` + two different non-null `sourceDevice` + values beyond `tolerance` (no winner — precedence is p8.6). `externalId` still pre-empts; within-tolerance still skips; same/unknown-device intra-batch disagreement stays deterministic `_stableOrder` behaviour, not a conflict. Rejected option B (silent data loss) + C (conflict semantics in the import service). Single-source behaviour byte-for-byte unchanged, regression-locked. Migration package shipped in-PR: g.dart regen + real `schema dump` → `drift_schema_v11.json` + `schema_v11.dart` + `_dumpedVersions += 11`, `migration_matrix_test` → v11 (all 3 loops; `source_device` NULL on pre-existing rows in both tables; non-null backup round-trip on a bbt row + a flow row), new `source_device_migration_test.dart` (v10 on disk → v11, `PRAGMA table_info` on both tables). `screen_nav` surface inventory 41→42. `decisions.md` + `phase-08.md` #### p8.2 + #### p8.6 edited on local main; `docs/health-platform-interop.md` "Device attribution" + `local-database.md` "Schema v11" + threat-model review-log entry all in-PR. **No `// SHORTCUT`.** Satisfies the exit gate's "≥1 third-party wearable". Depends p8.1a, p6.3. |
| p8.3 | DEFERRED | — | — | **Deferred to backlog** at the Phase 8 close (user's call 2026-09-07). The exit gate explicitly permits "ships OR recorded as a backlog deferral"; p8.2 already meets "≥1 third-party wearable". Direct wearable cloud API (WHOOP / Oura Cloud) — OAuth2 client + first `OlfHttpClient` egress + `flutter_secure_storage` token store + threat-model new-boundary entry (pull only, olf uploads nothing). Largest security-surface change in the project — its own future effort. `backlog.md` + `phase-08.md` #### p8.3. |
| p8.4 | CUT | — | — | 2026-09-06 — folded into **p8.3** at planning. WHOOP has no usable health-platform export, so a WHOOP connection is only achievable via the direct cloud API (p8.3's primary subject). Numbered placeholder, no independent work. |
| p8.5 | DONE | #86 | `3f41629` | Merged 2026-09-06, all 9 CI checks green. **Passive cycle-phase inference** from temperature — pure `core/lib/src/wearable/passive_phase_inference.dart`, `today`-injected, no `DateTime.now()`, no dep, no schema (derived-on-read). `inferPassivePhase(...)` → `PassivePhaseEstimate?` with a **one-sided** `enum PassivePhaseRead {ovulationLikelyPassed}` (no "did not ovulate", no fertility verdict — §6/§9(12) at the type level) + coarse `enum PassiveConfidence {low,medium,high}` (documented monotonic); picks ONE track (basal ≥9 readings else `sleepingWrist`, never blended), reuses p7.3 `thermalShift`, honest `null` on thin signal. `PassiveHrvSample`/`PassiveSleepSample` accepted in the signature but **not consumed** — HRV/sleep-ready API, **no new `HealthSampleType`/`HealthUnit`** so no §5 needed; ingestion is a later follow-up. `PassiveInformedPredictor implements Predictor` (interface unchanged — `CyclePrediction` gained an additive `copyWith`) re-anchors **only** `fertileWindow`; every next-period field passed through verbatim → backtest MAE/calibration structurally can't regress; no passive data → byte-identical to bare `AdaptivePredictor`. Correctable via p1.12 (decorator re-derives from `cycles`; a logged period/temp wins). **Backtester:** `synthetic_passive_signal.dart` gives each seeded profile a passive track; `passive_inference_backtest_test.dart` asserts no MAE/coverage/ovulation-error regression on every profile (+ exact pass-through); `passive_phase_inference_backtest_test.dart` scores the inference directly — regular profile detects >60% of cycles w/ median ovulation error ≤2d, every profile ≤3d, heavily-gapped stays silent. Non-diagnostic caption under the p1.12 wheel (`passivePhaseNote`, `reduceSpokenDetail`-redacted), content-locked by `passive_phase_copy_test.dart`. `screen_nav` seed feeds the existing calendar surface (count stays 42 — no new surface). `docs/passive-phase-inference.md` (new full model spec) + threat-model review-log entry. **1 `// SHORTCUT`:** `thermalShift` runs "3 over 6" on the reading *sequence* not calendar-spaced days (inherent p7.3 limit) — mitigated by density feeding confidence down; calendar-aware detector → p8.6+. Depends p8.1a (+p8.2), p3, p1.3, p1.12. Satisfies the exit gate's "passive inference measured vs the backtester, correctable". |
| close | DONE | #88 | `719ad04` | Merged 2026-09-07, all 9 CI checks green, `--delete-branch` succeeded (no stray remote branch — the close branch had no worktree pinning it). Exit-gate table filled, `overview.md` row 8 → DONE, `phase-08.md` phase-wide-truths + deferred-backlog + frozen per-slice record, `architecture.md` p8.6 refresh, p8.1b/p8.3 DEFERRED, `backlog.md` p8.1b/p8.3 entries; carried the 14-commit batched local-`main` bookkeeping stack. |
| p8.6 | DONE | #87 | `cb0a711` | Merged 2026-09-07, all 9 CI checks green. **Graceful multi-source arbitration** — pure `core` precedence policy (`source_precedence.dart` + shared vendor registry `known_devices.dart`, moved out of `app/device_label.dart`): tiers `manual`(3) > `attributedDevice`(2, `source_device` an exact hit in `known_devices`) > `sleepingWrist`(1, p8.1a `measurement_kind`) > `genericPlatform`(0). `HealthSample`/`LocalSampleView` gain a plain `bool isSleepingWrist`. Run inside the existing `ImportReconciler` decision point: same-`source` non-manual disagreement across **different tiers** → higher tier wins deterministically (`ReconciliationUpdate` up, new `ReconciliationSupersede` bucket down — dropped, not surfaced, not counted); **same tier + two recognised devices** → `crossDeviceDisagreement` for the user (exactly p8.2 — every p8.2 test unchanged); 3+ same-tier → **one** conflict folding the rest into `ReconciliationConflict.alsoContending`. `manual` disagreement is **always** a conflict, never auto-resolved. `conflict_review_screen` reworked to N source rows (each value, a "why" line, per-source "Use" action, keep-mine/dismiss, no bulk ops) + `reduceSpokenDetail` redaction. **§5 negotiation RESOLVED 2026-09-07 (Orchestrator "go", option a) — NO schema change:** loser not persisted by olf, stays in the OS health store (source of truth, idempotent import) → re-sync re-runs the policy; "delete winning source → runner-up wins" holds. Rejected option b (a `raw_health_readings` table → backlog). Acceptance criterion relaxed to "reversible at the platform level" + documented v1 limitation (changing precedence later won't retro-re-resolve without a re-pull). **PR gate: bounced once** — the first cut left `byTypeDay` stale after a *stored-row* tier-win, so a pre-existing lower-tier stored row + ≥2 higher-tier imports in one batch could invert tiers silently OR skip the p8.2 cross-device conflict; the fix (the feature branch's 2nd commit, squashed into `cb0a711`) makes the accepted reading occupy the slot for the rest of the pass (running-winner bookkeeping on the update path, "no blind update" withdraws the auto-update when a same-tier dispute follows) + 4 new stored-row-plus-≥2-incoming tests. **Known benign corner (not a blocker):** a stored lower-tier row + ≥2 *same-higher-tier-source* same-day readings with no `externalId` → first becomes an update, later ones inserts, and since `_apply` runs inserts before updates the `_stableOrder`-lowest value wins rather than the highest — near-impossible input (one wearable emitting multiple unlabelled same-day BBT values atop a generic row), arbitrary choice between equally-unlabelled values either way. `docs/health-platform-interop.md` "Multi-source precedence (p8.6)" + threat-model review-log entry (local, derived, reversible — no new asset/egress/dep/schema/CI). Surface count stays 42. **No net `// SHORTCUT`.** Depends p8.1a, p8.2, p8.5, p6.4. |

## In-flight PRs

- Nothing in flight. p8.1a #84 `5bee215`, p8.2 #85 `c36836f`, p8.5 #86 `3f41629`, p8.6 #87 `cb0a711` — all merged (squash, `--delete-branch` aborted on the worktree-pinned-local-ref error each time → remote branches still present, on the prune list). Next: the p8.1b/p8.3 build-vs-defer call, then either a dispatch or the Phase 8 close PR.

## Notes

- Abandoned/merged remote branches to prune (Orchestrator's `git push --delete` is classifier-blocked — user prunes): `feat/p6.4-two-way-sync` @ `55023d8` (abandoned, no PR); `feat/p6.4-two-way-sync-v2` (merged #69), `feat/p6.5-doctor-report` (merged #70), `feat/p7.1-postpartum-mode` @ `8a80bfa` (merged #73), `feat/p7.2a-pregnancy-mode` (merged #74), `feat/p7.2b-pregnancy-symptoms` (merged #76), `feat/p7.3-ttc-mode` (merged #77), `feat/p7.8-bc-switching` (merged #78), `feat/p7.4-pcos-mode` (merged #79), `feat/p7.7-perimenopause-mode` (merged #81), `feat/p7.5-endometriosis-mode` (merged #80), `feat/p7.6-pmdd-mode` (merged #82) — each `--delete-branch` aborted on the local-ref-pinned-by-worktree error, so the **remote** branch is also still there for #69/#70/#73/#74/#76/#77/#78/#79/#81/#80/#82; safe to delete (PR merged). `feat/p8.1a-apple-watch-wrist-temp` @ `9ae3be5` (merged #84), `feat/p8.2-wearable-device-provenance` @ `1a55fc2` (merged #85), `feat/p8.5-passive-phase-inference` @ `a8f366f` (merged #86), and `feat/p8.6-multi-source-arbitration` @ `2c01b16` (pre-squash branch tip; merged as `cb0a711` #87) — same abort, remotes still present, safe to delete. (`docs/plan-architecture-map` #75 deleted cleanly — no worktree.) Plus the pre-migration stale list: the Phase 5 `feat/p5.*` batch, `chore/release-v1.1.0`, `feat/p1.12-cycle-wheel`, `feat/p6.1-interop-foundation`, `feat/p6.2-healthkit-gateway`, `feat/p6.3-health-connect-gateway`. (`docs/phase-6-close`, `docs/plan-phase-7-expand`, `chore/plan-herdsman-migration` — now confirmed gone.) `docs/phase-8-close` (merged #88) — `--delete-branch` succeeded, no stray remote. `chore/release-v1.2.0` (merged #89 as `d8fb4f7`) — local deleted, **remote still present, safe to delete (PR merged, tag `v1.2.0` already cut).**
- v1.1.0 is the current release (tag on `6820cd2`). Versioning policy: `docs/plan/decisions.md` (2026-09-04). `1.x` = alpha until the last phase closes; `2.0.0` = beta cut at close.
- **state.md push policy (user decision 2026-09-06):** `protect-main` forbids Orchestrator direct-push to `main`. So `.herdsman/state.md` claim/merge edits are committed to **local `main` only** during a phase and folded into the **phase-close PR**. Between merges, `origin/main`'s copy of this file lags — local `main` is authoritative. After each Worker squash-merge, `git pull --rebase` the local bookkeeping commits onto the advanced `origin/main`.
