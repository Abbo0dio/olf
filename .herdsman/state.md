# herdsman state

Live build state. **Written only by the Orchestrator**, at dispatch and at merge.
Task status is here, never in `docs/plan/`. See `docs/plan/README.md` for the
plan layout and `.claude/skills/herdsman/` for the workflow.

## Now

**Current: `main` @ `1ca8f29` (r5a — #95, merged 2026-09-10) · next slice r5b — IN PROGRESS, Worker 1 (`../olf-wt/r5b`).**

**Phases 0–8 DONE. Feature roadmap PAUSED after Phase 8 (user decision 2026-09-07 — see `docs/plan/decisions.md`).** Phases 9–13 stay as the eventual roadmap — no new *feature* phase is expanded or dispatched.

**ACTIVE: UI / UX refresh pass (2026-09-08, user-requested).** Refinement of the current scope (phases 0–8) — information architecture, the day-log flow, visual consistency — run as dispatched slices `r1…r5` through the normal worktree → PR → gate → merge flow (it touches `screen_nav`, the a11y sweeps and `perf/log_period_tap_budget_test.dart`, so it needs the gate). Full breakdown: `docs/plan/ui-refresh.md`. This is NOT the feature roadmap resuming.

**Release `v1.2.0` — RELEASED 2026-09-07.** `chore: release v1.2.0` (#89) squash-merged as `d8fb4f7` on `main` (folded the Phase 8 DONE-lag backfill + this pivot's doc edits + `app/pubspec.yaml` `1.1.0+3` → `1.2.0+4`). Tag `v1.2.0` cut from `d8fb4f7`; `release.yml` built + published the **signed** `app-release.apk` (73,138,505 B) to <https://github.com/Abbo0dio/olf/releases/tag/v1.2.0> (not draft/prerelease, auto-generated notes). Brings the downloadable build up from `v1.1.0` (which predated Phases 6–8) to everything on `main` through the Phase 8 close. Minor bump per the 2026-09-04 versioning policy (new features shipped: Phases 6, 7, 8; still `1.x` alpha). `cut_release.sh` bailed twice on `api.github.com` network timeouts at its `--watch` step — CI on #89 was green (all 9), so the merge → tag → release-monitor were finished by hand. Local branch `chore/release-v1.2.0` deleted; **remote `chore/release-v1.2.0` stays for user prune.** On-device smoke items in `docs/release-checklist.md` (p5.4 discreet icon, p6.2/p6.3 health bridges, p6.5 doctor report) need real hardware — user's to run.

Phase 8 — Passive wearable integration — **CLOSED 2026-09-07** (#88 `719ad04`, exit gate MET). **`schemaVersion` 11.** Per-slice record frozen in `phase-08.md` (### Notes table); p8.1b + p8.3 DEFERRED to backlog. Phase 7 CLOSED (#83 `8f9a341`). Phase-boundary memory write done 2026-09-07.

## Workers

| W | agent    | tab    | status   | slice | worktree        |
| 1 | worker-1 | w6:t2  | building | r5b   | ../olf-wt/r5b (branch `feat/r5b-lists-empty-states-motion`) — r5a merged #95 |
| 2 | worker-2 | w6:tA  | idle     | —     | ../olf-wt/p7.6 (stale — merged #82) |
| 3 | worker-3 | w6:tB  | idle     | —     | ../olf-wt/p7.8 (stale — merged #78) |

Workers 2/3 idle with stale worktrees — torn down at their next dispatch. Worker 1's `../olf-wt/r4` and `../olf-wt/r5a` were already unlinked before #94 / #95 merged (no pin) — both `--delete-branch` runs fully succeeded, no prune entries.

## Tasks

**UI / UX refresh pass** (`docs/plan/ui-refresh.md`). Closed phases' per-slice records
are frozen in their `phases/phase-<n>.md` (Phase 8 = `phase-08.md`, Phase 7 =
`phase-07.md`, …) and dropped from here.

| ID | status | PR | SHA | log |
| r1 | DONE | #90 | `361ce97` | Merged 2026-09-08, all 9 CI checks green (incl. Perf budget). **`app/lib/src/prediction/forecast_area.dart` — new `ForecastArea` widget** owns the home "what goes where the forecast lives" decision: one child, fixed priority `bcRecalActive` → `perimenopauseGapSuppress` → `pregnancyModeOn`(nothing) → `prediction.isOverdue` → forecast → nothing. Priorities 1–3 reproduce the pre-r1 `!pregnancyModeOn && !bcRecalActive && !perimenopauseGapSuppress` guard **exactly** — zero behaviour change; the one consolidation win is recalibration + a perimenopause gap collapse to one card + one `secondaryLine` (`perimenopauseForecastSuppressedNote`, verbatim) instead of two stacked notes. `_PredictionCard` (pure verbatim move), `_RecalibrationNote` (gained optional `secondaryLine`, byte-identical when null), `_PerimenopausePausedNote` (verbatim) moved out of `period_calendar_page.dart`; call site swapped to one `ForecastArea(...)`. `_CorrectionNotice` + `_PregnancyStatusCard` untouched, still rendered directly above it. 13 tests in `forecast_area_test.dart` (per-branch + no-regression locks "recalibration/perimenopause-gap/pregnancy beats an OVERDUE prediction — no check-in" + real-home-screen multi-mode using the `perimenopause_screen_test` fixture + `_CorrectionNotice` stays above). **Gate bounced once** — first cut ordered overdue above the three suppression conditions (a user-visible behaviour delta); reorder fix `ce96e7d` on-branch. No new dep/permission/schema/egress/CI, no `screen_nav` surface, no `// SHORTCUT`. `architecture.md` prediction-card-gate clause updated. `app/`-only. |
| r2 | DONE | #91 | `8027eda` | Merged 2026-09-08, all 9 CI checks green (incl. Perf budget + both builds). **`app/lib/src/day_log/day_log_sheet.dart` — one `showDayLog(context, {date, lead})`** date-titled bottom sheet replaces `showFlowQuickLog` + `showSymptomDaySheet` (both files + their tests deleted). Sections as `ExpansionTile`s — **Flow · Symptoms · Temperature · Cervical fluid**, all immediate-upsert / no Save button, `initiallyExpanded` on the `lead` (period day / today → Flow, else Symptoms) — plus conditional **PMDD rating** / **Pain & flares** sections that mount the mode sheets' new `PmddRatingBody` / `EndometriosisPainBody` (extracted; `*Sheet` kept as a thin `Scaffold`/`AppBar` page wrapper; rating/pain models untouched; those two keep their own Save). `_TempDialog`/`_TempResult` + `writeBackBbt`/`writeBackFlow` carried verbatim — temperature (incl. p8.1a passive sub-label → typed-basal correction) and cervical-fluid behaviour identical. `period_calendar_page.dart` funnels all four entry points (calendar cell, cycle wheel, both summary chips) through one `_LoadedState._openDayLog(day, {lead})`; `onEditPeriodDates`/`onStartPeriod` gated on `_periodOn(day)`. **Perf gate held** — 2-tap flow path preserved (open period-day sheet → tap intensity), `perf/log_period_tap_budget_test.dart` uses its own canonical path, untouched. `screen_nav` net −1 (42→41): `symptom_day_sheet` + `flow_quick_log` surfaces → one `day_log` surface (opened from a period day; then expands Symptoms + Cervical fluid so those chip grids stay in the a11y sweep) + p8.1a passive-temp surface re-pointed with a Temperature-expand step. **Gate bounced once** — first cut left the collapsed Symptoms + Cervical-fluid sections out of every `screen_nav` fixture (a11y-sweep coverage gap ahead of r5a's `theme_contrast_test`); fix `525cc04` expands them in the `day_log` Surface. No new dep/permission/schema/egress/CI, no `// SHORTCUT`. `architecture.md` `day_log/` seam + PMDD/endo body-extract notes updated. `app/`-only. |
| r3a | DONE | #92 | `98cbaf4` | Merged 2026-09-08, all 9 CI checks green (incl. Perf budget). **`app/lib/src/app_shell.dart` — the nav model:** `appTabIndexProvider` (`StateProvider<int>`, ephemeral) + `AppShell` — outer `Scaffold`/`AppBar`/`NavigationBar` [Home · Calendar · Patterns], `IndexedStack` body (per-tab scroll retention for free), Calendar-tab `AppBar` overflow → demoted Medications + Pregnancy-loss/birth; Settings a top-right gear. `home_page.dart` reduced to the DB-gate → `const AppShell()`. `PeriodCalendarView` gains `HomeVariant` (`home`/`calendar`) — one `_LoadedState`, two layouts (`_HomeBody` slim: wheel + `_PeriodStatusLine` + `_ModeChipStrip` + `ForecastArea` + `_ThisCycleCard`→Patterns + `_RecentActivity` ≤3 + "See all in Calendar"; `_CalendarBody`: month grid + horizontal-swipe month nav + jump-to-today + lazy year-grouped `SliverList.builder` history). `patterns/patterns_view.dart` = empty placeholder (r3b fills). `modeScreen` made public. **§5 resolution 1 (perf-gate fast path, option (a)):** `_LoadedState._markTodayAsPeriodStart` — one-tap direct write in the day-log sheet's Flow section (today, non-period only), **byte-parity verified** against `period_editor.dart:_save` (`existing==null` → `addPeriod(PeriodDraft(start: dateOnly(today), end: null))`, nothing else), same `_runHistoryEdit`→`_reportOutcome`("Period saved.")→correction-notice path, no editor surface. Perf test re-pointed FAB→"Mark today as period start"; `maxTaps = 2` + feedbackBudget on the ack unchanged ("Day 1" moved past settle — wheel redraw isn't synchronous; feedback-latency guarantee intact). **§5 resolution 2 (APK-size baseline):** `Perf budget` failed at +5.09 % over `.github/perf-baseline.json`'s stale `v1.0.1` figure; ruled a baseline correction (Phases 6–8 dep growth, `v1.2.0` shipped at +4.63 %, r3a's own delta +0.45 %) → `apk_release_bytes` → `73138505` in standalone commit `09c1172` + `decisions.md` 2026-09-08 entry + backlog follow-up. **`screen_nav` 41→42:** `period_calendar_page — with cycle history` → `calendar_tab — month grid + cycle history` (switches tab); `— cycle wheel (active phase)` → `home_tab — cycle wheel (active phase)`; new `patterns_tab — placeholder`; meds/day-log/manage-symptoms surfaces re-pathed to the "Log" FAB; every moved/new surface stays swept; `reduce_spoken_detail_test` redaction re-pointed onto the recent-activity rows + Calendar day cell. **CI failed once** (Perf budget / APK size — the stale baseline, fixed `09c1172`); review otherwise passed first read. Disclosed corner-cuts (all accepted): `_CycleStatsCard` stays on Home until r3b; Calendar FAB opens `showDayLog(today)` not a selected day (no selection model → r5c, backlog); `_HistoryRow` edit is row-tap→sheet (one extra tap); overdue-card period-start still uses the editor (backlog). No `// SHORTCUT`, no new dep/permission/schema/egress; only non-`app/` change = the `.github/perf-baseline.json` bump. `architecture.md` `period/` + `app_shell.dart`/`patterns/` seam updated. Depends r1, r2. |
| r3b | DONE | #93 | `aa16a65` | Merged 2026-09-08, all 9 CI checks green. **`app/lib/src/patterns/patterns_view.dart`** — `PatternsView` becomes a `ListView`: accuracy row → `AccuracyPage`; "Your cycles" = `_CycleStatsCard` **moved verbatim from `period_calendar_page.dart`** (−114 there, incl. the Home usage + the r3a "kept on Home" comment); "Basal temperature" = one `BbtChart` per cycle with ≥2 basal readings (no cap); "Symptoms across your cycle" = p7.4 `CorrelationChart` per `enoughData` symptom, **section hidden wholesale until one qualifies**; "Modes" = an "Open <mode>" row per enabled mode (`modeScreen`) + "Life-stage & condition modes" → `ModesPage`. **`pcos_mode_providers.dart` → `patterns/patterns_providers.dart`** — its sole provider `pcosCorrelationsProvider` (never PCOS-specific) renamed `symptomPhaseCorrelationsProvider`, derivation body unchanged; `pcos_screen.dart` = import + watch-name swap only; no dangling refs. **`settings_page.dart`** — the Cycle→"Prediction accuracy" row + the whole "Modes" section removed (3 imports dropped); Cycle header kept for "Pregnancy loss & birth"; nothing else touched (r4 owns the rest). Thin-data honesty verified by `patterns_tab_test.dart` (5 tests). `reduceSpokenDetail`: correlations category text + chart label redact to "symptom". **`screen_nav` 42→43:** `patterns_tab — placeholder` → `patterns_tab — populated` + `patterns_tab — thin data`; `accuracy_page` + every mode-screen surface re-pointed through the Patterns tab via a new `_openFromPatterns` helper; sweeps green. pcos/perimenopause screen tests correctly split the gap-wording assertions (the "wide date range" note stays on Home's prediction card; the softened gap line follows `_CycleStatsCard` to Patterns). **Clean gate — no bounce.** No `// SHORTCUT`, no new dep/permission/schema/egress/CI; `app/`-only. Carried to r5a/r5b: `_CycleStatsCard` is still a hand-rolled radius-12 `Container` (r5a); `PatternsView`'s eager `ListView` + per-cycle BBT loop (r5b). `architecture.md` `patterns/` + `pcos_screen` refs updated. Depends r3a. |
| r4 | DONE | #94 | `be37094` | Merged 2026-09-10, all 9 CI checks green. **Clean gate — no bounce** (gate was still open at the machine restart; completed on the resume pass: full read verified `data_and_sharing_screen.dart` is a verbatim lift of the health block + helpers out of `settings_page.dart`, acceptance criteria all met). `settings_page.dart` → settings-only: **removed** the `Cycle` section (its only row "Pregnancy loss & birth" is double-homed in the Calendar-tab overflow since r3a — pointer dropped, empty header removed), the `Data` (Backup) and `Apps & export` (Export + health) sections + their helper methods, six provider watches, seven imports. **Kept** Appearance, Pronouns, Privacy & security, Accessibility, Notifications. **New `app/lib/src/settings/data_and_sharing_screen.dart`** — `DataAndSharingScreen` (`ConsumerWidget`, AppBar "Data & sharing"), reached from one Settings row (`Data` → "Data & sharing"): **On this device** → Backup & restore (`BackupPage`); **Sharing** → Export report for a doctor (`ExportReportScreen`), then the whole health-platform block when a platform is available — Connect a health app (connect/disconnect dialogs), Sync now, the p8.1a passive Apple-Watch line, the p8.2 per-device provenance rows, and "N differences to review" → `ConflictReviewScreen`. Helpers (`_healthSubtitle`, `_ago`, `_connectHealth`, `_syncHealth`, `_disconnectHealth`, `_summarySentence`) moved verbatim; the sync-summary `reduceSpokenDetail` redaction unchanged. Direct deep links untouched (`privacy_education_screen.dart`'s "Open Backup & restore" still pushes `BackupPage`; retention purge stays in Settings → Privacy); the Privacy basics "export a backup first" copy re-pointed through Data & sharing. **`screen_nav` 43→44:** new `data_and_sharing` surface (Settings → "Data & sharing"); the two `settings_page — Apps & export …` surfaces re-pointed as `data_and_sharing — health app connected` / `— per-device list`; `backup_page`, `export_report_screen`, `conflict_review_screen — differences to review` re-pointed via new `_openFromDataAndSharing` / `_openDataAndSharing` helpers; `pregnancy_events_page` opens from the Calendar overflow; all six sweeps (`screen_guidelines` / `semantics_labels` / `text_scaling@1/1.5/2` / `theme_contrast` / `keyboard_nav` / `focus_order`) + dark mode iterate `screenSurfaces` and stay green. **Tests:** new `data_and_sharing_test.dart` (screen is one Settings row; Backup & restore / doctor export / "differences to review" → review screen all open from it; health block absent with no platform, connect tile shows with one); `settings_page_test.dart` rewritten to the r4 "Settings holds only settings + the one Data & sharing entry" check; re-pointed `backup_flow_test`, `export_report_flow_test`, `health/{connect_health_flow,sync_status,conflict_review}_test`, `pregnancy/pregnancy_events_test`, `modes/mode_offer_test` (the two `find.text('Apps & export')` assertions → a `Data & sharing` AppBar check). `flutter test` (app) 692 pass; analyze + format clean; `core/` untouched (`git diff origin/main -- core/` empty). **No `// SHORTCUT`** from this slice (the two in-diff markers — `report_pdf.dart`, `health_providers.dart` — are pre-existing). No new dep/permission/schema/egress/CI; `app/`-only. `architecture.md` `settings/` line updated (bookkeeping commit). Depends r3b. |
| r5a | DONE | #95 | `1ca8f29` | Merged 2026-09-10, all 9 CI checks green. **Clean gate — no bounce.** Pure visual pass, zero copy changes. **8 hand-rolled `Container` + `BorderRadius.circular(12)` cards → theme `Card`** (olf_theme.dart: elevation 0, `surfaceContainerLow`, radius **16**). Each `Card` runs `semanticContainer: false` + `margin: EdgeInsets.zero` under the parent `Semantics(container: true)` (purely visual — the parent scroll views already apply the 16px inset + inter-card `SizedBox` gap; shape/elevation/colour come from the theme): `_PredictionCard` (forecast + overdue branches), `_RecalibrationNote`, `_PerimenopausePausedNote` (forecast_area.dart); `_ThisCycleCard` (InkWell splash clipped via `Card.clipBehavior: antiAlias`), `_CorrectionNotice`, `_PregnancyStatusCard` (period_calendar_page.dart); **`_CycleStatsCard`** (patterns_view.dart — the r3b-carried item, done). **One accent per screen:** forecast card keeps `primaryContainer`; every other card drops its `*Container` tint → neutral `surfaceContainer*` with `onSecondaryContainer` → `onSurfaceVariant`; `_ThisCycleCard`/`_CycleStatsCard` go `surfaceContainerHighest` → theme `surfaceContainerLow` (both still neutral, per criterion). **Overdue:** leading `Icons.event_busy_outlined` + thin **`error`** edge (`RoundedRectangleBorder` side 1.5) on the unchanged `primaryContainer` — no `tertiaryContainer` bg flip; text `onTertiaryContainer` → `onPrimaryContainer`; never colour-only (icon + 'Period check-in' heading retained). *(Gate note — token judgement: dispatch/`ui-refresh.md` said a thin `errorContainer` accent; Worker used `error`. Accepted: `errorContainer` is a low-chroma container tint that would be near-invisible as a 1.5px border on `primaryContainer`, while `error` is the high-signal accent meant for affordances; the edge is redundant with icon + text so use-of-colour holds; **no token changed anywhere** — every fg/bg pair in play (`onSurfaceVariant`/`surfaceContainerLow`, `onPrimaryContainer`/`primaryContainer`) is an already-asserted `theme_contrast_test` role pair.)* **One a11y detail:** `_CorrectionNotice`'s reason lines now wrapped in `ExcludeSemantics` — the outer `liveRegion`/`_spokenLabel` Semantics already speaks the whole note; the new Card/Material boundary would have split the reason `Text`s into their own nodes (double-counted under `find.bySemanticsLabel` in `forecast_area_test`). Excluding them restores the single announced node; the dismiss `IconButton` stays fully semantic. Body copy byte-identical everywhere (all copy/content tests untouched and green). **New `app/test/theme/card_radius_test.dart`** (gate belt-and-braces on top of the untouched `theme_contrast_test`): theme radius 16 in both brightnesses; forecast = one `Card` on `primaryContainer`; overdue = no `tertiaryContainer`, leading icon, r16 `error`-side shape; recal/perimenopause notes neutral `Card`s; Home + Patterns tabs → every `Card` r16 + **no `BoxDecoration` left at `circular(12)`** tree-wide. `forecast_area_test` / `prediction_card_test` / `correction_notice_test` unchanged and green (they assert text/icons/semantics, not decoration). No `// SHORTCUT` (0 in diff), no new dep/permission/schema/egress/CI, **no `screen_nav` change (0 surfaces)**, no `pubspec` change, `core/` untouched, `perf/log_period_tap_budget_test.dart` untouched + green. `flutter test` app 699 green + core 806 green; analyze/format clean. `app/`-only + the fold-forward docs commit. Depends r3a. |
| r5b | IN PROGRESS | — | — | Dispatched to Worker 1 2026-09-10 — worktree `../olf-wt/r5b`, branch `feat/r5b-lists-empty-states-motion` off `origin/main` (`1ca8f29`), folds held local-`main` commit (`git cherry-pick origin/main..main`). Lists (lazy), one shared empty-state widget (≈8 sites), subtle `AnimatedSize`/fade on conditional cards gated on reduce-motion. **Carried from r3b: `PatternsView` is an eager `ListView(children:)` with a per-cycle BBT-chart loop — convert to `ListView.builder`/slivers.** `ui-refresh.md` #r5b. Depends r3a. |
| r5c | TODO | — | — | Calendar interactions: horizontal month swipe + jump-to-today, chevrons kept. `ui-refresh.md` #r5c. Depends r3a. |

<details><summary>Phase 8 rows (frozen — see <code>phase-08.md</code>)</summary>

| ID | status | PR | SHA | log |
| p8.1a | DONE | #84 | `5bee215` | Merged 2026-09-06, all 9 CI checks green (incl. `Build (macos-latest, ios)` — Swift bridge compiles). Apple Watch **passive wrist-temperature data path**, no companion app, no new dep. iOS bridge maps `HKQuantityTypeIdentifier.appleSleepingWristTemperature` (read-only, `#available(iOS 16)` guarded, no write/share auth) → `HealthSampleType.wristTemperature`; `health_import.dart` re-types to `basalBodyTemperature` **only at the reconcile boundary** (unchanged `ImportReconciler` `(type,day)` match), stores the row `measurementKind: sleepingWrist`, never pushes it back out. **§5 negotiation RESOLVED 2026-09-06 (Orchestrator "go") — one additive column, `schemaVersion` 9→10:** `measurement_kind TEXT NOT NULL DEFAULT 'basal'` on `bbt_entries` (`enum BbtMeasurementKind {basal, sleepingWrist}`); **rejected** a dedicated `wrist_temperature` table (would force a reconciler change) and unmarked `tempCelsius` reuse. **Condition MET — zero behaviour change:** all 5 `bbt_entries` temp readers filter `measurement_kind = 'basal'` (`thermalShift`, `dailyFertilityScore`, `bbt_chart`, `clinical_report` temp series, `drift_logging_activity_repository`); a `core` test asserts mixed-kind history === basal-only for shift / score / report. `source_device` **deferred to p8.2**; HRV/sleep **deferred to p8.5**. Migration package shipped in-PR: g.dart regen + real `schema dump` → `drift_schema_v10.json` + `generated/schema_v10.dart` + `_dumpedVersions += 10`, `migration_matrix_test` → v10 (`measurement_kind=='basal'` on pre-existing rows + `sleepingWrist` backup round-trip), new `wrist_temp_migration_test.dart` (v9 on disk → v10, `PRAGMA table_info`). `bbt_entries` already in `BackupService.tableOrder` + `RetentionService.deleteWhere` — no additions. `screen_nav` surface inventory 40→41. threat-model review-log entry + `local-database.md` "Schema v10". **1 `// SHORTCUT`:** conflict-review "take incoming" for a wrist-vs-manual conflict stores it as `basal` (pure `ReconciliationConflict` carries no kind) — rare manual path, re-correctable, deferred to p8.5. Depends p6.1, p6.2, p1.6. |
| p8.1b | DEFERRED | — | — | **Deferred to backlog** at the Phase 8 close (user's call 2026-09-07). Conditional slice — the exit gate ships it only "if its §5 plumbing clears"; the four hard requirements are met without it. watchOS SwiftUI companion + complication, App Group / `WatchConnectivity`, no network. New Xcode target + HealthKit entitlement + CI compile lane. `backlog.md` + `phase-08.md` #### p8.1b. |
| p8.2 | DONE | #85 | `c36836f` | Merged 2026-09-06, all 9 CI checks green (incl. both builds). **Oura + Garmin via the health platform** + device provenance — no vendor SDK, no OAuth, no network, no new permission (`pubspec.lock` / manifest / entitlements / plist all untouched). Nullable `source_device` TEXT on `daily_flows` + `bbt_entries`, `schemaVersion` 10→11, migration `if (from < 11 && to >= 11) { if (from >= 3) …dailyFlows; if (from >= 5) …bbtEntries; }` (`daily_flows` v3 / `bbt_entries` v5). iOS `withDevice()` reads `HKSource.name`/`HKDevice.name` off already-fetched samples; Android reads `dataOrigin.packageName`; `RawHealthSample.toWire()` never emits it (inbound provenance only). `device_label.dart` pure prettifier; `contributingDevicesProvider` + per-device "Apps & export" list (`reduceSpokenDetail`-redacted, hidden when all-manual). **§5 negotiation RESOLVED 2026-09-07 (Orchestrator "go", option A):** one minimal additive `ImportReconciler` clause — `sourceDevice` threaded through `HealthSample`/`LocalSampleView`; `inserts` indexed into `byTypeDay` as they accumulate; `ConflictReason.crossDeviceDisagreement` for a `(type,day)` match that is same `source` + two different non-null `sourceDevice` + values beyond `tolerance` (no winner — precedence is p8.6). `externalId` still pre-empts; within-tolerance still skips; same/unknown-device intra-batch disagreement stays deterministic `_stableOrder` behaviour, not a conflict. Rejected option B (silent data loss) + C (conflict semantics in the import service). Single-source behaviour byte-for-byte unchanged, regression-locked. Migration package shipped in-PR: g.dart regen + real `schema dump` → `drift_schema_v11.json` + `schema_v11.dart` + `_dumpedVersions += 11`, `migration_matrix_test` → v11 (all 3 loops; `source_device` NULL on pre-existing rows in both tables; non-null backup round-trip on a bbt row + a flow row), new `source_device_migration_test.dart` (v10 on disk → v11, `PRAGMA table_info` on both tables). `screen_nav` surface inventory 41→42. `decisions.md` + `phase-08.md` #### p8.2 + #### p8.6 edited on local main; `docs/health-platform-interop.md` "Device attribution" + `local-database.md` "Schema v11" + threat-model review-log entry all in-PR. **No `// SHORTCUT`.** Satisfies the exit gate's "≥1 third-party wearable". Depends p8.1a, p6.3. |
| p8.3 | DEFERRED | — | — | **Deferred to backlog** at the Phase 8 close (user's call 2026-09-07). The exit gate explicitly permits "ships OR recorded as a backlog deferral"; p8.2 already meets "≥1 third-party wearable". Direct wearable cloud API (WHOOP / Oura Cloud) — OAuth2 client + first `OlfHttpClient` egress + `flutter_secure_storage` token store + threat-model new-boundary entry (pull only, olf uploads nothing). Largest security-surface change in the project — its own future effort. `backlog.md` + `phase-08.md` #### p8.3. |
| p8.4 | CUT | — | — | 2026-09-06 — folded into **p8.3** at planning. WHOOP has no usable health-platform export, so a WHOOP connection is only achievable via the direct cloud API (p8.3's primary subject). Numbered placeholder, no independent work. |
| p8.5 | DONE | #86 | `3f41629` | Merged 2026-09-06, all 9 CI checks green. **Passive cycle-phase inference** from temperature — pure `core/lib/src/wearable/passive_phase_inference.dart`, `today`-injected, no `DateTime.now()`, no dep, no schema (derived-on-read). `inferPassivePhase(...)` → `PassivePhaseEstimate?` with a **one-sided** `enum PassivePhaseRead {ovulationLikelyPassed}` (no "did not ovulate", no fertility verdict — §6/§9(12) at the type level) + coarse `enum PassiveConfidence {low,medium,high}` (documented monotonic); picks ONE track (basal ≥9 readings else `sleepingWrist`, never blended), reuses p7.3 `thermalShift`, honest `null` on thin signal. `PassiveHrvSample`/`PassiveSleepSample` accepted in the signature but **not consumed** — HRV/sleep-ready API, **no new `HealthSampleType`/`HealthUnit`** so no §5 needed; ingestion is a later follow-up. `PassiveInformedPredictor implements Predictor` (interface unchanged — `CyclePrediction` gained an additive `copyWith`) re-anchors **only** `fertileWindow`; every next-period field passed through verbatim → backtest MAE/calibration structurally can't regress; no passive data → byte-identical to bare `AdaptivePredictor`. Correctable via p1.12 (decorator re-derives from `cycles`; a logged period/temp wins). **Backtester:** `synthetic_passive_signal.dart` gives each seeded profile a passive track; `passive_inference_backtest_test.dart` asserts no MAE/coverage/ovulation-error regression on every profile (+ exact pass-through); `passive_phase_inference_backtest_test.dart` scores the inference directly — regular profile detects >60% of cycles w/ median ovulation error ≤2d, every profile ≤3d, heavily-gapped stays silent. Non-diagnostic caption under the p1.12 wheel (`passivePhaseNote`, `reduceSpokenDetail`-redacted), content-locked by `passive_phase_copy_test.dart`. `screen_nav` seed feeds the existing calendar surface (count stays 42 — no new surface). `docs/passive-phase-inference.md` (new full model spec) + threat-model review-log entry. **1 `// SHORTCUT`:** `thermalShift` runs "3 over 6" on the reading *sequence* not calendar-spaced days (inherent p7.3 limit) — mitigated by density feeding confidence down; calendar-aware detector → p8.6+. Depends p8.1a (+p8.2), p3, p1.3, p1.12. Satisfies the exit gate's "passive inference measured vs the backtester, correctable". |
| close | DONE | #88 | `719ad04` | Merged 2026-09-07, all 9 CI checks green, `--delete-branch` succeeded (no stray remote branch — the close branch had no worktree pinning it). Exit-gate table filled, `overview.md` row 8 → DONE, `phase-08.md` phase-wide-truths + deferred-backlog + frozen per-slice record, `architecture.md` p8.6 refresh, p8.1b/p8.3 DEFERRED, `backlog.md` p8.1b/p8.3 entries; carried the 14-commit batched local-`main` bookkeeping stack. |
| p8.6 | DONE | #87 | `cb0a711` | Merged 2026-09-07, all 9 CI checks green. **Graceful multi-source arbitration** — pure `core` precedence policy (`source_precedence.dart` + shared vendor registry `known_devices.dart`, moved out of `app/device_label.dart`): tiers `manual`(3) > `attributedDevice`(2, `source_device` an exact hit in `known_devices`) > `sleepingWrist`(1, p8.1a `measurement_kind`) > `genericPlatform`(0). `HealthSample`/`LocalSampleView` gain a plain `bool isSleepingWrist`. Run inside the existing `ImportReconciler` decision point: same-`source` non-manual disagreement across **different tiers** → higher tier wins deterministically (`ReconciliationUpdate` up, new `ReconciliationSupersede` bucket down — dropped, not surfaced, not counted); **same tier + two recognised devices** → `crossDeviceDisagreement` for the user (exactly p8.2 — every p8.2 test unchanged); 3+ same-tier → **one** conflict folding the rest into `ReconciliationConflict.alsoContending`. `manual` disagreement is **always** a conflict, never auto-resolved. `conflict_review_screen` reworked to N source rows (each value, a "why" line, per-source "Use" action, keep-mine/dismiss, no bulk ops) + `reduceSpokenDetail` redaction. **§5 negotiation RESOLVED 2026-09-07 (Orchestrator "go", option a) — NO schema change:** loser not persisted by olf, stays in the OS health store (source of truth, idempotent import) → re-sync re-runs the policy; "delete winning source → runner-up wins" holds. Rejected option b (a `raw_health_readings` table → backlog). Acceptance criterion relaxed to "reversible at the platform level" + documented v1 limitation (changing precedence later won't retro-re-resolve without a re-pull). **PR gate: bounced once** — the first cut left `byTypeDay` stale after a *stored-row* tier-win, so a pre-existing lower-tier stored row + ≥2 higher-tier imports in one batch could invert tiers silently OR skip the p8.2 cross-device conflict; the fix (the feature branch's 2nd commit, squashed into `cb0a711`) makes the accepted reading occupy the slot for the rest of the pass (running-winner bookkeeping on the update path, "no blind update" withdraws the auto-update when a same-tier dispute follows) + 4 new stored-row-plus-≥2-incoming tests. **Known benign corner (not a blocker):** a stored lower-tier row + ≥2 *same-higher-tier-source* same-day readings with no `externalId` → first becomes an update, later ones inserts, and since `_apply` runs inserts before updates the `_stableOrder`-lowest value wins rather than the highest — near-impossible input (one wearable emitting multiple unlabelled same-day BBT values atop a generic row), arbitrary choice between equally-unlabelled values either way. `docs/health-platform-interop.md` "Multi-source precedence (p8.6)" + threat-model review-log entry (local, derived, reversible — no new asset/egress/dep/schema/CI). Surface count stays 42. **No net `// SHORTCUT`.** Depends p8.1a, p8.2, p8.5, p6.4. |

</details>

## In-flight PRs

- **Nothing in flight.** r5a merged #95 `1ca8f29` (2026-09-10, clean gate — cards & colour, all 9 CI checks green). r5b dispatched to Worker 1 — PR not yet open.

## Notes

- Abandoned/merged remote branches to prune (Orchestrator's `git push --delete` is classifier-blocked — user prunes): `feat/p6.4-two-way-sync` @ `55023d8` (abandoned, no PR); `feat/p6.4-two-way-sync-v2` (merged #69), `feat/p6.5-doctor-report` (merged #70), `feat/p7.1-postpartum-mode` @ `8a80bfa` (merged #73), `feat/p7.2a-pregnancy-mode` (merged #74), `feat/p7.2b-pregnancy-symptoms` (merged #76), `feat/p7.3-ttc-mode` (merged #77), `feat/p7.8-bc-switching` (merged #78), `feat/p7.4-pcos-mode` (merged #79), `feat/p7.7-perimenopause-mode` (merged #81), `feat/p7.5-endometriosis-mode` (merged #80), `feat/p7.6-pmdd-mode` (merged #82) — each `--delete-branch` aborted on the local-ref-pinned-by-worktree error, so the **remote** branch is also still there for #69/#70/#73/#74/#76/#77/#78/#79/#81/#80/#82; safe to delete (PR merged). `feat/p8.1a-apple-watch-wrist-temp` @ `9ae3be5` (merged #84), `feat/p8.2-wearable-device-provenance` @ `1a55fc2` (merged #85), `feat/p8.5-passive-phase-inference` @ `a8f366f` (merged #86), and `feat/p8.6-multi-source-arbitration` @ `2c01b16` (pre-squash branch tip; merged as `cb0a711` #87) — same abort, remotes still present, safe to delete. (`docs/plan-architecture-map` #75 deleted cleanly — no worktree.) Plus the pre-migration stale list: the Phase 5 `feat/p5.*` batch, `chore/release-v1.1.0`, `feat/p1.12-cycle-wheel`, `feat/p6.1-interop-foundation`, `feat/p6.2-healthkit-gateway`, `feat/p6.3-health-connect-gateway`. (`docs/phase-6-close`, `docs/plan-phase-7-expand`, `chore/plan-herdsman-migration` — now confirmed gone.) `docs/phase-8-close` (merged #88) — `--delete-branch` succeeded, no stray remote. `chore/release-v1.2.0` (merged #89 as `d8fb4f7`) — local deleted, **remote still present, safe to delete (PR merged, tag `v1.2.0` already cut).** `feat/r1-forecast-area` (#90 `361ce97`), `feat/r2-day-log-sheet` (#91 `8027eda`), `feat/r3a-nav-shell` (#92 `98cbaf4`) and `feat/r3b-patterns-tab` (#93 `aa16a65`) — each `--delete-branch` aborted on its `../olf-wt/r1` / `r2` / `r3a` / `r3b` worktree pin; **remotes still present, safe to delete (PRs merged).** `feat/r4-settings-data-sharing` (#94 `be37094`) — `--delete-branch` **fully succeeded** (its `../olf-wt/r4` pin was already unlinked), remote gone, no prune entry. `feat/r5a-cards-colour` (#95 `1ca8f29`) — `--delete-branch` **fully succeeded** (its `../olf-wt/r5a` pin was already unlinked), remote gone, no prune entry.
- **v1.2.0 is the current release** (tag on `d8fb4f7`, 2026-09-07). Versioning policy: `docs/plan/decisions.md` (2026-09-04). `1.x` = alpha until the last phase closes; `2.0.0` = beta cut at close.
- **state.md push policy (user decision 2026-09-06):** `protect-main` forbids Orchestrator direct-push to `main`. So `.herdsman/state.md` + `docs/plan/` claim/merge edits are committed to **local `main` only** and folded into the next Worker PR (the dispatch tells the Worker to cherry-pick `origin/main..main` onto its branch). Between merges, `origin/main`'s copy lags — local `main` is authoritative. After each Worker squash-merge, `git reset --hard origin/main` then re-commit the merge bookkeeping (no phase-close PR to ride up with during the UI-refresh pass). Held now: this r5a-merge bookkeeping commit (state.md r5a→DONE / r5b→IN PROGRESS + Workers/In-flight rows + `architecture.md` theme-`Card` notes) — folds into r5b's PR (the dispatch tells Worker 1 to cherry-pick `origin/main..main` onto its branch).
