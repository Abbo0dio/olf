# herdsman state

Live build state. **Written only by the Orchestrator**, at dispatch and at merge.
Task status is here, never in `docs/plan/`. See `docs/plan/README.md` for the
plan layout and `.claude/skills/herdsman/` for the workflow.

## Now

Phase 8 — Passive wearable integration — **IN PROGRESS** · `origin/main` @ `8f9a341` (local `main` +2 bookkeeping: `557297a` Phase 7 CLOSED + this Phase 8 expand/seed). **`schemaVersion` 9.** Phase 7 CLOSED (#83 `8f9a341`, exit gate MET, per-slice record frozen in `phase-07.md`). Phase 8 planned 2026-09-06 — `docs/plan/phases/phase-08.md` expanded from the 6-line stub into full rows: **p8.1a** Apple Watch wrist-temp data path (no companion app) · **p8.1b** watchOS SwiftUI companion (new Xcode target — §5) · **p8.2** Oura/Garmin via the health platform + device provenance (no dep/network) · **p8.3** direct wearable cloud API (OAuth + first `OlfHttpClient` egress — §5-heavy, maybe backlog) · **p8.4** folded into p8.3 (WHOOP has no platform export) · **p8.5** passive cycle-phase inference (pure `core`, measured vs the p3 backtester, correctable) · **p8.6** multi-source arbitration (extends `ImportReconciler` + `conflict_review_screen`). **p8.1a DISPATCHED to Worker 1.** Workers 2/3 idle.

## Workers

| W | agent    | tab    | status   | slice | worktree        |
| 1 | worker-1 | w6:t2  | building | p8.1a | ../olf-wt/p8.1a (fresh off `origin/main` @ `8f9a341`) — tears down stale `../olf-wt/p7.5` |
| 2 | worker-2 | w6:tA  | idle     | —     | ../olf-wt/p7.6 (stale — merged #82) |
| 3 | worker-3 | w6:tB  | idle     | —     | ../olf-wt/p7.8 (stale — merged #78) |

Workers 2/3 idle with stale Phase 7 worktrees — torn down at their next dispatch.

## Tasks

Current phase (8). Phase 7 per-slice record is **frozen in `phase-07.md` (### Notes
table)** — all 8 mode slices + close DONE (#73–#83, close `8f9a341`), dropped from
here at the Phase 7 close. Phase 6 frozen in `phase-06.md`, Phase 5 in `phase-05.md`.
Full Phase 8 slice scope: `docs/plan/phases/phase-08.md`.

| ID | status | PR | SHA | log |
| p8.1a | IN PROGRESS | #84 (draft) | — | worker-1 · `../olf-wt/p8.1a` · `feat/p8.1a-apple-watch-wrist-temp` off `origin/main` @ `8f9a341`. Apple Watch **passive wrist-temperature data path**, no companion app, no new dep. Bridge reads `HKQuantityTypeIdentifier.appleSleepingWristTemperature` → `HealthSampleType.wristTemperature`; `HealthImportService` re-types to `basalBodyTemperature` for the unchanged `ImportReconciler` `(type,day)` match, stamps `measurementKind: sleepingWrist` on apply. **§5 negotiation RESOLVED 2026-09-06 (Orchestrator "go") — one additive column, `schemaVersion` 9→10:** approved `measurement_kind TEXT NOT NULL DEFAULT 'basal'` on `bbt_entries` (`enum BbtMeasurementKind {basal, sleepingWrist}`); **rejected** a dedicated `wrist_temperature` table (would force a reconciler change → breaks the "unchanged reconciler conflicts wrist-vs-manual-BBT" criterion) and **rejected** unmarked `tempCelsius` reuse (conflates sleeping-wrist w/ basal — `thermalShift`/BBT chart/doctor PDF). **Condition:** every existing `bbt_entries` temp reader (`thermalShift`, `dailyFertilityScore`, `ClinicalReport`, p1.6 chart) filters to `measurement_kind='basal'` → zero behaviour change. `source_device` **deferred to p8.2**; HRV/sleep **deferred to p8.5**. `decisions.md` + `phase-08.md` #### p8.1a written (this commit). Migration deliverable in #84: g.dart regen + real `schema dump` → `drift_schema_v10` + `schema_v10.dart` + `_dumpedVersions` += 10, `migration_matrix_test` → v10 (`measurement_kind=='basal'` on pre-existing rows + backup round-trip w/ a `sleepingWrist` row), new `wrist_temp_migration_test.dart` (v9 on disk → v10, `PRAGMA table_info`). `bbt_entries` already in `BackupService.tableOrder` + `RetentionService.deleteWhere` — no additions. threat-model review-log entry, `local-database.md` "Schema v10". Depends p6.1, p6.2, p1.6. |
| p8.1b | TODO | — | — | watchOS **SwiftUI companion app** — glanceable current-cycle-phase view (mirrors p1.12 `currentCyclePhase`) + ≥1 complication. New Xcode watch target + own HealthKit entitlement + own CI build coverage = **§5 stop before any code**. Phone↔watch via App Group / `WatchConnectivity`, no network, no cycle data leaves the device. Pure codec unit-tested; Swift UI compile-validated in CI (no signing); real device = p0.5 smoke. Sequenced after p8.1a. Depends p8.1a, p1.12. |
| p8.2 | TODO | — | — | **Oura + Garmin via the health platform** + device provenance — no vendor SDK, no OAuth, no network. **Adds the nullable `source_device` text column** to `daily_flows` + `bbt_entries` (§5 schema change — deferred here from p8.1a; full migration deliverable in-PR, the v10 `measurement_kind` precedent), populated from `HKSource`/`HKDevice` (iOS) / Health Connect origin (Android) so readings attribute to Oura/Garmin/Apple Watch. Per-device status surface in "Apps & export". Deterministic cross-device reconciliation (no dupe/clobber; material disagreement → reviewable conflict). `docs/health-platform-interop.md` updated. **Can satisfy the exit gate's "≥1 third-party wearable" alone.** Garmin's direct Health API (server-to-server partnership) explicitly out of scope. Depends p8.1a, p6.3. |
| p8.3 | TODO | — | — | **Direct wearable cloud API** (Oura Cloud and/or WHOOP) — the §5-heavy path for wearables that don't write to a health platform (WHOOP writes ~nothing). **§5 STOP up front:** OAuth2 client (creds not committed) + outbound HTTPS via `OlfHttpClient` (first real use — pin/verify per p2.6) + token store in `flutter_secure_storage` + threat-model new-boundary entry (new egress, but olf **uploads nothing** — pull only). Orchestrator rules on vendor(s), scope, and whether it ships in Phase 8 or **defers to backlog**. Strong deferral candidate if p8.2 already meets the gate. Depends p8.2, p2.6. |
| p8.4 | CUT | — | — | 2026-09-06 — folded into **p8.3** at planning. WHOOP has no usable health-platform export, so a WHOOP connection is only achievable via the direct cloud API (p8.3's primary subject). Numbered placeholder, no independent work. |
| p8.5 | TODO | — | — | **Passive cycle-phase inference** from temperature (+ HRV / sleep) — pure `core/lib/src/wearable/passive_phase_inference.dart`, `today`-injected, no `DateTime.now()`. Reads the passive multi-signal history + logged periods + current `CyclePrediction` → a phase estimate with explicit confidence + honest `null` on thin/gappy signal; reuses p7.3 `thermalShift` where it fits (no second detector). It is an **input to** `AdaptivePredictor` (seam unchanged), the inferred phase is **correctable** through the p1.12 flow (a correction always wins). **Measured against the Phase 3 backtester** — synthetic profiles gain a passive-signal track; combined predictor must not regress MAE/calibration on any seeded profile. **No alarm/diagnosis (§6, §9(12))** — content test. No ML dep; explainable rule/threshold pipeline. Depends p8.1a (+p8.2), p3, p1.3, p1.12. |
| p8.6 | TODO | — | — | **Multi-source arbitration** — pure `core` precedence policy: manual always wins; among automatic sources a documented deterministic order (dedicated basal device > Apple Watch sleeping wrist > generic platform sample). Every raw reading retained + provenance-tagged; the resolved day value is a **derived read**, not a destructive merge (change precedence / delete winning source → re-resolves, no data loss). Extends `conflict_review_screen` (p6.4) to N sources (which is winning + why; keep-mine / use-this-source / dismiss; no bulk ops). Retention + backup cover every raw row. Reconciler hardening, not a new engine. Depends p8.1a, p8.2, p8.5, p6.4. |

## In-flight PRs

- **p8.1a** — Worker 1 building on `feat/p8.1a-apple-watch-wrist-temp` off `origin/main` @ `8f9a341`. Draft PR expected shortly; poke will come as "Worker 1: PR #<n> ready" (or a §5 NEGOTIATION on the storage model / `source_device` provenance — both pre-flagged in the dispatch).
- Phase 7 close PR #83 merged `8f9a341` — nothing else in flight.

## Notes

- Abandoned/merged remote branches to prune (Orchestrator's `git push --delete` is classifier-blocked — user prunes): `feat/p6.4-two-way-sync` @ `55023d8` (abandoned, no PR); `feat/p6.4-two-way-sync-v2` (merged #69), `feat/p6.5-doctor-report` (merged #70), `feat/p7.1-postpartum-mode` @ `8a80bfa` (merged #73), `feat/p7.2a-pregnancy-mode` (merged #74), `feat/p7.2b-pregnancy-symptoms` (merged #76), `feat/p7.3-ttc-mode` (merged #77), `feat/p7.8-bc-switching` (merged #78), `feat/p7.4-pcos-mode` (merged #79), `feat/p7.7-perimenopause-mode` (merged #81), `feat/p7.5-endometriosis-mode` (merged #80), `feat/p7.6-pmdd-mode` (merged #82) — each `--delete-branch` aborted on the local-ref-pinned-by-worktree error, so the **remote** branch is also still there for #69/#70/#73/#74/#76/#77/#78/#79/#81/#80/#82; safe to delete (PR merged). (`docs/plan-architecture-map` #75 deleted cleanly — no worktree.) Plus the pre-migration stale list: the Phase 5 `feat/p5.*` batch, `chore/release-v1.1.0`, `feat/p1.12-cycle-wheel`, `feat/p6.1-interop-foundation`, `feat/p6.2-healthkit-gateway`, `feat/p6.3-health-connect-gateway`. (`docs/phase-6-close`, `docs/plan-phase-7-expand`, `chore/plan-herdsman-migration` — now confirmed gone.)
- v1.1.0 is the current release (tag on `6820cd2`). Versioning policy: `docs/plan/decisions.md` (2026-09-04). `1.x` = alpha until the last phase closes; `2.0.0` = beta cut at close.
- **state.md push policy (user decision 2026-09-06):** `protect-main` forbids Orchestrator direct-push to `main`. So `.herdsman/state.md` claim/merge edits are committed to **local `main` only** during a phase and folded into the **phase-close PR**. Between merges, `origin/main`'s copy of this file lags — local `main` is authoritative. After each Worker squash-merge, `git pull --rebase` the local bookkeeping commits onto the advanced `origin/main`.
