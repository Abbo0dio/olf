# herdsman state

Live build state. **Written only by the Orchestrator**, at dispatch and at merge.
Task status is here, never in `docs/plan/`. See `docs/plan/README.md` for the
plan layout and `.claude/skills/herdsman/` for the workflow.

## Now

Phase 7 — Life-stage & condition modes — IN PROGRESS · `origin/main` @ `3d8d788` (local `main` +12 bookkeeping) · **7 of 8 slices DONE** — p7.1 (#73), p7.2 (#74 + #76), p7.3 (#77), p7.4 (#79), **p7.7 (#81 `3d8d788`)**, p7.8 (#78). Also landed: `docs/plan/architecture.md` (#75). **Only p7.5 + p7.6 left.** In flight: **p7.5** (Worker 1 — endometriosis; NEGOTIATION RESOLVED — dedicated `pain_entries` table, `schemaVersion` 7→8, in PR #80 — `docs/plan/decisions.md` 2026-09-06 + `phase-07.md` #### p7.5 storage block; the phase's schema-bump slice). **p7.6 (PMDD) is the last slice** — blocked on p7.5's `SymptomSeverity` enum; dispatches to Worker 2 or 3 once #80 merges. Then Phase 7 close. Workers 2 + 3 idle until then. **When p7.5 rebases onto post-#81 main: `screen_nav.dart` count → 34** (33 from #81 + p7.5's +1), + append-only conflicts in `mode_catalog.dart` / `modes_page.dart` / `period_calendar_page.dart` / `cycle_format.dart` (compose the endo flag alongside `pcosMode`/`perimenopauseMode`/`bcRecalActive`).

## Workers

| W | agent    | tab    | status   | slice | worktree        |
| 1 | worker-1 | w6:t2  | building | p7.5  | ../olf-wt/p7.5 |
| 2 | worker-2 | w6:tA  | idle     | —     | ../olf-wt/p7.7 (stale — teardown at p7.6 dispatch) |
| 3 | worker-3 | w6:tB  | idle     | —     | ../olf-wt/p7.8 (stale) |

## Tasks

Current phase (7) + previous phase (6). Phase 6 rows are the frozen record
(detail in `docs/plan/phases/phase-06.md`); they drop at the Phase 7 close.
Phase 5 detail is frozen in `phase-05.md`.

| ID | status | PR | SHA | log |
| p6.1 | DONE | #65 | `ed81ac5` | 2026-09-05 — interop foundation: `HealthPlatformGateway` + `FakeHealthPlatformGateway` + pure `ImportReconciler` + schema v6→v7 provenance cols (`source`/`externalId` on bbt_entries + daily_flows). `core`-only, no dep. Doc fixes folded in `e7b308d`. |
| p6.2 | DONE | #66 | `76c31cd` | 2026-09-05 — Apple HealthKit gateway (iOS): hand-rolled `olf/health` MethodChannel + Swift `HealthKitBridge`, no dep, no SDK-floor bump, ATS untouched. `setTemp`/`setFlow` gained `source`/`externalId`; `+allEntries()`/`allFlows()`. |
| p6.3 | DONE | #67 | `de46108` | 2026-09-05 — Android Health Connect gateway: hand-rolled Kotlin bridge on `MainActivity`, same wire contract. `androidx.health.connect:connect-client:1.1.0` as a Gradle dep (not a pub package — `pubspec.lock` clean); `minSdk` 24→26; 4 `android.permission.health.*`. Shared `MethodChannelHealthGateway` base + `sourceTag`. New `docs/health-platform-interop.md`. (Plan had said IN REVIEW; it merged.) |
| p6.4 | DONE | #69 | `2db45db` | 2026-09-06 — Two-way sync: write-back after every flow/BBT log/edit (`externalId` sticky across an edit so a previously-imported day updates its platform record in place, no dupe; edit still flips `source`→`manual` — the p6.1 deferral), per-source status surface (connected · last-sync "N min ago" · counts · "N differences to review"; `reduceSpokenDetail`-redacted), new `conflict_review_screen.dart` (list + keep-mine / use-theirs / dismiss, no bulk ops). **v1 = manual "Sync now" only** — on-open/resume sync deferred (backlog). Retention respected both directions (purge-before-sync via `retentionController.sweepNow()`; cutoff clamps import window + filters push-out). Reconciler: value-already-agrees is now a skip regardless of source (absorbs the write-back echo); manual-value protection unchanged. In-memory conflict store (`// SHORTCUT`, backlog). No dep / no schema / no permission / no manifest / no CI change. CI Format bounced once (worker's local `dart format` under-reported — `analysis_options.yaml` env bug); fixed in `9d3d403`, squashed into `2db45db`. core 573 / app 426. |
| p6.5 | DONE | #70 | `ea585b1` | 2026-09-06 — Doctor-ready offline PDF report. `pdf ^3.12.0` added (Apache-2.0, direct main) — all §5 conditions met: dependency-audit green with the 7-pkg transitive subtree, pure Dart, no plugin, no SDK-floor bump, no `printing`; APK-size budget green. Pure `core/lib/src/export/clinical_report.dart` (`ClinicalReport` + `buildClinicalReport`, `generatedOn` injected, no `DateTime.now()`); `SymptomRepository.allTypes()` (archived included). `app`: `report_pdf.dart` (single A4, Helvetica core font, hand-drawn temp chart, `// SHORTCUT _ascii()` non-Latin glyph drop → backlog), `report_providers.dart` (purge-before-export via `sweepNow()`, neutral `olf-report-YYYY-MM-DD.pdf`), `export_report_screen.dart` (range picker + preview, new `screen_nav.dart` surface #20). `BackupFileGateway.writeBackup`→ generic `saveFile()` — backup + report share the SAF seam. "Apps & export" section now always shown. threat-model + release-checklist updated. No schema/permission/manifest/CI change. core 586 / app 436. |
| Phase 6 close | DONE | #71 | `6f8c40c` | 2026-09-06 — Phase 6 CLOSED; exit gate MET (all 5 clauses → slice + PR# + SHA in `docs/plan/phases/phase-06.md`); `overview.md` row 6 → DONE. Phase-wide: one schema bump (p6.1 v6→v7), `health` pkg evaluated+rejected (both gateways hand-rolled), `pdf` added for p6.5, `core` stayed Flutter-free / `DateTime.now()`-free. 4 backlog deferrals (on-open sync, delete propagation, persisted conflict store, bundled Unicode PDF font). |
| p7.1 | DONE | #73 | `4e84c30` | 2026-09-06 — Pregnancy-loss/birth/postpartum flows + the **mode framework**. `core`: `LifeStageMode` enum (8 modes, each owns `app_settings` key `mode.<name>` — NO schema change) + pure `derivePostpartumCycleReturn` (clock-injected, over existing period + p1.11 pregnancy-event history; `PostpartumSettling` {awaitingFirstPeriod, firstCycleLogged, settling, stillVariable}; `regularity` honest `null` until ≥2 completed post-event cycles; reuses p1.3 `CycleStats`/`CycleRegularity`, no new predictor). `app`: Settings→"Modes"→`ModesPage` (all 8 listed, toggle on/off, data survives — delivered as a Settings row→page, not 8 inline tiles, to avoid pushing "Apps & export" off-viewport); `mode_offer.dart` calm dismissible post-log offer (opt-in only, enables only on explicit "Turn it on"); `postpartum_screen.dart` cycle-return view (`reduceSpokenDetail`-redacted, "still settling" predictor note, disclaimer); `support_resources_screen.dart` + content (loss vs birth, bundled non-clinical text, no phone-home links, not-a-medical-device + find-care lines). `_PregnancyStatusCard` gained an "Open postpartum view" button when the mode is on. 3 new `screen_nav` surfaces; 5 a11y sweeps green; threat-model Phase 7 entry (derived view, no new asset/egress/schema). **Correlation-chart widget deferred to p7.4** (no per-day series in p7.1 to validate a reusable API — Worker's call, dispatch-permitted; p7.4 row already carries it). No dep / no schema / no permission / no manifest / no CI change. No `// SHORTCUT`. core 602 / app 465. |
| p7.2a | DONE | #74 | `b058299` | 2026-09-06 — Pregnancy mode part 1: gestational-age core + week view. `core`: `gestational_age.dart` (`PregnancyReferenceKind` LMP/dueDate/conception → `PregnancyStartReference.lmpAnchor` single conversion seam; `GestationalAge` completed-weeks+day+trimester; `gestationalAgeAsOf(ref, asOf:)` clock-injected, returns `null` when asOf precedes anchor — honest "check your dates", never a negative week; Naegele 280d / conception −14d), `pregnancy_week_notes.dart` (`kPregnancyWeekNotes` 43 entries wk 0–42, terse non-clinical non-directive, no links; tone-locked by content test + p1.9 lint). `app`: `pregnancy_mode_providers.dart` (start ref stored as `app_settings` `pregnancy.start_reference` = `"<kind>|<yyyy-mm-dd>"` — **no schema change**; toggle-off keeps the row), `pregnancy_mode_format.dart` (+ redacted `reduceSpokenDetail` variant), `pregnancy_start_sheet.dart`, `pregnancy_week_screen.dart` (3 states: needs-ref / ref-in-future / week view; disclaimer always shown), `mode_catalog`/`modes_page` route pregnancy → its screen. 2 new `screen_nav` surfaces; sweeps green. threat-model p7.2a entry. Enablement = Modes-section-only (Option A). No dep / no schema / no permission / no manifest / no CI change. No `// SHORTCUT`. core 625 / app 481. |
| p7.2b | DONE | #76 | `5fb2734` | 2026-09-06 — Pregnancy mode part 2, **app-only** (no core/pubspec touch). `pregnancy_symptoms.dart`: `kPregnancySymptomNames` (15 plain non-clinical gender-neutral names) + `PregnancySymptomsScreen` — logs "today" through the existing p1.5 `SymptomRepository` (`addType`/`setSymptom`/`symptomsOn`); a curated name not in the catalogue is created as an ordinary custom symptom on first tap, one matching a built-in binds to that row (dedup), in-flight guard vs double-tap; also lists the user's own symptoms; disclaimer + `reduceSpokenDetail`. `PregnancyWeekScreen` gets a "Log pregnancy symptoms" button. `period_calendar_page.dart`: `_PredictionCard` block gated `&& !pregnancyModeOn` (`lifeStageModeEnabledProvider(pregnancy)`) — forecast card hidden while pregnancy mode is on, returns when off; widget gate only, no core change. `pregnancy_events_page.dart`: on a p1.11 loss/birth, if pregnancy mode was on → `setLifeStageModeEnabled(pregnancy, false)` (keeps the start reference) before the existing `offerPostpartumMode` chain. New `pregnancy_symptoms_screen` sweep surface. No dep/schema/permission/manifest/CI change. No `// SHORTCUT`. app 494 / core 611. |
| p7.3 | DONE | #77 | `0553215` | 2026-09-06 — TTC mode. `core`: new `bbt/thermal_shift.dart` (pure retrospective 3-over-6 shift detector — `today` injected, explicitly NOT a forecaster; one `// SHORTCUT` — operates on the reading sequence not calendar-spaced days, a logging gap can mistime the shift → backlog) + `modes/daily_fertility_score.dart` (`dailyFertilityScore{cycles,bbt,mucus,prediction,today,day}` → `DailyFertilityScore?`; fixed day-relative shape curve on the p3 predictor's ovulation estimate, re-centred by an observed thermal shift, floored up by observed fertile-quality mucus, shrunk toward a neutral baseline + widened band on thin/irregular history; score clamped 1–99, never 0/100; `<2` completed cycles → null → empty state; `FertilityConfidence` band + `FertilityFactor` "what shaped this" list; `dailyFertilityScores(...,days)` series). **NO second engine** — reuses `CyclePrediction.fertileWindow`, `observedFertileWindow`, `CycleStats`, `fertileDays{Before,After}Ovulation`. `app`: `ttc_providers.dart` (`DateTime.now()` at the edge), `ttc_format.dart` (`ttcModeDisclaimer` = not-a-medical-device + explicit "not contraception guidance — do not use these scores to try to avoid pregnancy"), `ttc_screen.dart` (score + band + framing, fertile window **as a range**, next-8-day outlook, "what shaped today's score", empty state; `reduceSpokenDetail` on the score), `mode_catalog`/`modes_page` wire ttc → its screen. `screen_nav` +2 surfaces (26→28). No dep / no schema / no permission / no manifest / no CI change. threat-model p7.3 entry NOT added by the slice (not a p7.3 acceptance criterion, not CI-enforced) — Orchestrator adds it at the phase-close threat-model walk. core 611+? / app +? . |
| p7.4 | DONE | #79 | `276420d` | 2026-09-06 — PCOS mode + the reusable correlation core. `core`: `cycle_phase.dart` refactored (four-segment builder extracted to `_phaseSegments` — `currentCyclePhase` behaviour verified identical) + new `cyclePhaseTimeline(cycles,{today})` (retrospective twin, anchored on real logged next-period dates, reuses `lutealPhaseDays` + fertile offsets) + new `modes/cycle_phase_correlation.dart` (`PhaseEvent{day,category}` opaque stream → `PhaseCorrelation`; thresholds ≥2 completed cycles / ≥5 placed days; concentration = per-phase-day rate ≥1.5× mean, so luteal isn't flagged for being longest; NO p-values/causal/diagnostic language; deterministic, `today` injected — **p7.5/p7.6/p7.7 parameterise this**). `app`: `correlation_chart.dart` (the p7.1-deferred shared cycle-phase-banded bar — colour never sole signal, one Semantics summary, dark-mode via `ColorScheme` roles), `pcos_screen.dart` + `pcos_mode_providers.dart` (`cyclePhaseCorrelations` over p1.5 symptom log + cycles, `DateTime.now()` at edge, archived names kept) + `pcos_correlation_format.dart` (explicit `pcosNotSymptomCheckerLine` — anti-Flo §9(12): "it cannot tell you whether you have PCOS — only a clinician can assess"), `period_calendar_page.dart` + `cycle_format.dart` PCOS copy-softening (`pcosMode` flag on `summariseStats`/`cycleLengthNote`/cards — **presentation only, gated on `pcosModeOn`, `deriveCycles`/`CycleStats`/predictor untouched**). `mode_catalog`/`modes_page` wire pcos → screen. `screen_nav` +2 surfaces. threat-model p7.4 entry present. New `cycle_phase.dart`→`robust_predictor.dart` import for 3 shared constants (same pattern p7.3 set). No dep/schema/permission/manifest/CI change. No `// SHORTCUT`. core 637 / app 511. Depends p7.1, p1.5, p1.3, p1.12. |
| p7.5 | IN PROGRESS | #80 | — | worker-1 · `../olf-wt/p7.5` · `feat/p7.5-endometriosis-mode` off `276420d`. Endometriosis mode. **Negotiation resolved 2026-09-06 — Orchestrator approved a dedicated table (p1.5 presence-only model can't hold an ordered scale + free-text note + region).** In PR #80: new `pain_entries` table (PK `date`, one row/day: `intensity` non-null · `region` nullable · `note` nullable TEXT · `isFlare` bool · `createdAt`/`updatedAt`), `schemaVersion` **7→8** additive `if (from < 8) createTable(painEntries)`, regen `.g.dart` + `drift_schemas/v8` + `test/db/generated/schema_v8`, `migration_matrix_test`→8, new `pain_migration_test.dart`, backup round-trip, `backup_service` `tableOrder` += `pain_entries`. New pure `core` `enum SymptomSeverity` (`.rank`/`.label`) — reusable ordered scale, **p7.6 reuses it verbatim**; `region` = fixed `core` `enum PainRegion` (not user-editable). Flare↔phase correlation reuses `cyclePhaseCorrelations` unchanged (Pain/Flare as `PhaseEvent` categories). Pain-over-time view reuses `CorrelationChart`. threat-model p7.5 entry names the new asset (free-text pain notes + structured rows, SQLCipher-encrypted at rest, no new egress/permission/CI); `release-checklist.md` "Schema change" block updated. `reduceSpokenDetail` on pain detail; new `screen_nav` surface(s); sweeps; dark mode. Depends p7.1, p7.4, p1.5. |
| p7.6 | TODO | — | — | PMDD mode — daily multi-symptom rating (ordered scale from p7.5 or shared `core`; reuse p7.5 storage choice) + cycle-overlay chart (ratings aligned by cycle day/phase across N cycles) + descriptive luteal-vs-follicular summary via p7.4 core. NO DRSP score / diagnostic threshold. Small fixed rating set for v1. Depends p7.1, p7.4, p7.5, p1.12. |
| p7.7 | DONE | #81 | `3d8d788` | 2026-09-06 — Perimenopause / menopause mode. `core/lib/src/modes/perimenopause_transition.dart` (pure, `today` injected): `derivePerimenopauseTransition({periods, today})` → `PerimenopauseTransitionRead?` (null when nothing logged) over `deriveCycles` + `CycleStats` + gap logic. Output is **two enums** — `PerimenopauseVariabilityTrend {notEnoughData, noClearChange, becomingLessRegular}` (compare cycle-length spread + skip count across the earlier vs later half of the recent 12-cycle window; ≥5-day widening or more skips → less regular) + `PerimenopauseStageHint` — plus plain facts (`daysSinceLastPeriod`, `earlier/laterSpreadDays`, `recentSkipCount`, `twelveMonthsSinceLastPeriod` = `>= 365`). **NO numeric score / %/0–100** (§9(12) hard line) — content test asserts it + that the copy says "not a score, not a diagnosis". Predictor/`deriveCycles`/`CycleStats` untouched. `app`: `perimenopause_screen.dart` + `perimenopause_mode_providers.dart` (`perimenopauseTransitionProvider` + `perimenopauseCorrelationsProvider` reusing p7.4 `cyclePhaseCorrelations` + `CorrelationChart`, filtered to `kPerimenopauseSymptomNames` ∪ user's non-built-in symptoms — no new storage), `perimenopause_format.dart` (copy + bundled name list; "12 months" surfaced factually with disclaimer), `cycle_format.dart` parallel `perimenopauseMode` flag (PCOS phrasing wins if both), `period_calendar_page.dart` `perimenopauseModeOn` + `perimenopauseGapSuppress` (from `read.longGapsBetweenCycles`) → `_PerimenopausePausedNote` replaces the forecast card past a long gap / 12+ months; guard now `!pregnancyModeOn && !bcRecalActive && !perimenopauseGapSuppress`; else a wider-interval note. `mode_catalog`/`modes_page` wired. `screen_nav` +2 surfaces (comment says 34, actual 33 — p7.5 rebase reconciles). threat-model p7.7 entry present. No dep/schema/permission/manifest/CI change. No `// SHORTCUT`. core 679 / app 556. Depends p7.1, p7.4, p1.3. |
| p7.8 | DONE | #78 | `91df394` | 2026-09-06 — Birth-control switching support (guided recalibration). §5 pre-check cleared: p1.7 `BirthControlEntries` already carries dated `startedOn`/`endedOn` — no schema change. `core/lib/src/modes/birth_control_recalibration.dart` (pure, `today` injected): `BirthControlMethodHormonal.isHormonal` (pill/patch/ring/injection/implant = true; iud/condom/other = false — no per-type pharmacology per scope) + `deriveBirthControlRecalibration({entries,cycles,today,dismissedAt,windowDays=90,postSwitchCyclesNeeded=3})` → `BirthControlRecalibration?` (started = current open hormonal entry's `startedOn`; stopped = latest ended-hormonal `endedOn`; future switch → null; `active` ⇔ within window AND < N post-switch non-gap cycles AND not dismissed; dismissal sticky until a newer switch). Predictor untouched — AdaptivePredictor has no time-scoped exclusion seam, so the forecast is **withheld** during the window (acceptance-criterion fallback), not rebuilt. `app`: `birth_control_recalibration_providers.dart` (gated on `mode.birthControlSwitch`; `mode.birthControlSwitch.dismissedAt` ISO-8601 KV row — no schema), `_content` (bundled starting-vs-stopping / withdrawal-bleed explainer, no links, content test asserts no phone-home URL), `_screen` (guided explainer), `period_calendar_page.dart` `_RecalibrationNote` card replaces `_PredictionCard` while active — gate composed `!pregnancyModeOn && !bcRecalActive`; `meds_providers.dart` +`birthControlHistoryProvider` (`watchAll()`). `screen_nav` +1 surface. threat-model p7.8 entry present. Rebased once onto `0553215` (append-only conflicts with #77 on the 3 mode-wiring files) + a 1-line surface-count comment fix. No dep/schema/permission/manifest/CI change. No `// SHORTCUT`. core 203-new-lines / app +? . |

## In-flight PRs

_(none — p7.5 + p7.7 dispatched, PRs not opened yet)_

## Notes

- Abandoned/merged remote branches to prune (Orchestrator's `git push --delete` is classifier-blocked — user prunes): `feat/p6.4-two-way-sync` @ `55023d8` (abandoned, no PR); `feat/p6.4-two-way-sync-v2` (merged #69), `feat/p6.5-doctor-report` (merged #70), `feat/p7.1-postpartum-mode` @ `8a80bfa` (merged #73), `feat/p7.2a-pregnancy-mode` (merged #74), `feat/p7.2b-pregnancy-symptoms` (merged #76), `feat/p7.3-ttc-mode` (merged #77), `feat/p7.8-bc-switching` (merged #78), `feat/p7.4-pcos-mode` (merged #79), `feat/p7.7-perimenopause-mode` (merged #81) — each `--delete-branch` aborted on the local-ref-pinned-by-worktree error, so the **remote** branch is also still there for #69/#70/#73/#74/#76/#77/#78/#79/#81; safe to delete (PR merged). (`docs/plan-architecture-map` #75 deleted cleanly — no worktree.) Plus the pre-migration stale list: the Phase 5 `feat/p5.*` batch, `chore/release-v1.1.0`, `feat/p1.12-cycle-wheel`, `feat/p6.1-interop-foundation`, `feat/p6.2-healthkit-gateway`, `feat/p6.3-health-connect-gateway`. (`docs/phase-6-close`, `docs/plan-phase-7-expand`, `chore/plan-herdsman-migration` — now confirmed gone.)
- v1.1.0 is the current release (tag on `6820cd2`). Versioning policy: `docs/plan/decisions.md` (2026-09-04). `1.x` = alpha until the last phase closes; `2.0.0` = beta cut at close.
- **state.md push policy (user decision 2026-09-06):** `protect-main` forbids Orchestrator direct-push to `main`. So `.herdsman/state.md` claim/merge edits are committed to **local `main` only** during a phase and folded into the **phase-close PR**. Between merges, `origin/main`'s copy of this file lags — local `main` is authoritative. After each Worker squash-merge, `git pull --rebase` the local bookkeeping commits onto the advanced `origin/main`.
